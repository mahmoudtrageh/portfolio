/*
 | Dashboard entry point.
 |
 | Deliberately almost empty. Livewire 4 ships its own Alpine build, so
 | importing and starting alpinejs here (as resources/js/app.js does for the
 | public site) would run two instances and silently break every wire:
 | directive on the page.
 |
 | Anything the dashboard needs beyond Livewire goes here.
 */
