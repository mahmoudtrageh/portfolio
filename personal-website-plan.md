# خطة بناء الموقع الشخصي — محمود طه (Backend Engineer)

مرجع الإلهام: mahmoudadel.online (نفس البنية والإحساس، لكن بمحتوى ونبرة تناسب مهندس Backend مش Frontend).

---

## 1. الفكرة العامة والهوية

- **الاسم/العلامة:** محمود طه — أو Mahmoud Taha | Backend Engineer (ممكن تربطها بـ Binaa Soft لاحقًا كـ "brand" خفيف في الفوتر).
- **الرسالة الأساسية (Value Proposition):** مهندس Backend متخصص في Laravel/PHP، بنى وشغّل أنظمة SaaS متعددة المستأجرين (Multi-tenant) على Production حقيقي — مش مجرد مشاريع تعليمية.
- **الفرق عن الموقع المرجعي:** هو Frontend (React/Next.js/GSAP)، إنت Backend (Laravel/Filament/DevOps). الأرقام والإثباتات لازم تكون من نوع تاني: عدد الـ tenants، مدة uptime، عدد السيرفرات المُدارة، حجم البيانات، تحسين تكلفة Cloud، إلخ.
- **اللغة:** ثنائية اللغة إجباريًا — عربي (RTL) وإنجليزي (LTR) بالكامل، زي الموقع المرجعي بالظبط، مع زرار تبديل لغة واضح في الـ Navbar (`/` للعربي و `/en` للإنجليزي مثلًا).
- **الوضع الليلي/النهاري (Dark/Light Mode):** خاصية أساسية مطلوبة من أول إطلاق — زرار تبديل واضح في الـ Navbar (أيقونة شمس/قمر)، مع حفظ اختيار المستخدم (localStorage) واحترام تفضيل النظام (`prefers-color-scheme`) كقيمة افتراضية أول زيارة.
- **الشخصية البصرية:** جدي وتقني (خطوط عريضة، ألوان داكنة + لون أساسي واحد مميز)، مش لازم أنيميشن GSAP معقد زي الموقع المرجعي — الأداء والوضوح أهم لمهندس Backend.

---

## 2. البنية (Sitemap) — نفس هيكل الموقع المرجعي

| الصفحة | الغرض |
|---|---|
| `/` الرئيسية | Hero + مشاريع مميزة + مسار مهني مختصر + مهارات + CTA |
| `/about` عني | القصة الكاملة (جيوفيزياء → Backend، متجر الأدوات، دايف باث) |
| `/projects` المشاريع | كل المشاريع بتفاصيل + دراسات حالة |
| `/projects/[slug]` دراسة حالة | صفحة مستقلة لكل مشروع كبير (BoxesVDR، Linkatik، IronLink) |
| `/testimonials` التوصيات | آراء عملاء/زملاء (لو متاحة) |
| `/contact` تواصل | فورم + روابط تواصل مباشرة |
| (اختياري) `/blog` أو `/writing` | مقالات تقنية أو ربط بقناة اليوتيوب و DevPath.ar |

---

## 3. محتوى صفحة الهيرو (Hero Section)

- **Eyebrow:** "٦+ سنوات خبرة Backend... من صيدلية أدوات في قرية بالغربية لأنظمة SaaS بتخدم عملاء حقيقيين."
- **العنوان الرئيسي:** "مهندس Backend، بابني أنظمة SaaS متعددة المستأجرين بتستحمل الإنتاج الحقيقي."
- **السطر الفرعي (Stats قابلة للإثبات):**
  - مثال: "بنيت وشغّلت أكثر من X منصة SaaS على Production • أدرت هجرة بنية تحتية كاملة عبر ٣ سيرفرات • خفّضت تكلفة Cloud بحوالي ٢٠-٣٠٪"
  - (استبدل بأرقام حقيقية دقيقة من شغلك: WithaqVDR migration، Huawei Cloud audit، عدد الـ tests في PetroApp، إلخ)
- **الحالة:** "متاح لفرص جديدة" + الموقع "طنطا، الغربية، مصر" أو "عن بُعد".
- **أزرار CTA:** [عرض المشاريع] و [تحميل السيرة الذاتية] (عندك نسخة PDF جاهزة بالفعل من الشغل السابق).
- **صورة شخصية** احترافية (نفس أسلوب الموقع المرجعي بصورة بورتريه واضحة).

---

## 4. المشاريع المميزة (Featured Projects) — اقتراح المحتوى

اختر ٣-٤ أقوى مشاريع بنفس أسلوب الكروت (تصنيف + سنة + وصف + تقنيات + رابط دراسة حالة):

1. **BoxesVDR / WithaqVDR** — SaaS
   - وصف: منصة غرفة بيانات افتراضية (Virtual Data Room) متعددة المستأجرين على Huawei Cloud، شاملة توجيه Subdomain لكل عميل (tenant wildcard routing)، وأنظمة أمان (auth_request gates)، ونشر عبر GitHub Actions.
   - تقنيات: Laravel, Multi-tenancy, Nginx, Huawei Cloud, Cloudflare, CI/CD
   - إنجاز رقمي: هجرة دومين كاملة عبر ٣ بيئات (production/dev/staging) بدون downtime + توفير في تكلفة Cloud.

2. **Linkatik** — SaaS
   - وصف: منصة Bio-link (رابط حيوي) بواجهة Next.js وخلفية Laravel، مُدارة على سيرفر Hetzner واحد بعد توحيد بنية تحتية كانت متفرقة على عدة خدمات.
   - تقنيات: Laravel, Next.js, PM2, Nginx, Certbot/SSL automation
   - إنجاز رقمي: توحيد (consolidation) عدة خوادم DigitalOcean/Plesk في سيرفر واحد + أتمتة تجديد شهادات SSL.

3. **IronLink** (قيد التطوير) — Marketplace + سلسلة تعليمية
   - وصف: سوق B2B لموردي الأدوات الصناعية، مبني بـ Laravel وFilament، مصمم كمنتج SaaS حقيقي وفي نفس الوقت كسلسلة فيديوهات تعليمية تغطي المعمارية والاختبارات والـ DevOps وتكامل الذكاء الاصطناعي.
   - تقنيات: Laravel, Filament, Testing, DevOps, LLM integration

4. **Koomiz** — SaaS
   - وصف: نظام إدارة أعمال متعدد المستأجرين مدعوم بالذكاء الاصطناعي، مع خاصية OCR للفواتير.
   - تقنيات: Laravel, AI/OCR, Multi-tenancy

> ملاحظة: زي الموقع المرجعي بالظبط — كل كارت لازم يوضّح **رقم أو نتيجة ملموسة**، مش بس وصف تقني. دور على أرقام حقيقية عندك (عدد tenants، مدة تشغيل بدون توقف، عدد المستخدمين، حجم التوفير في التكلفة).

---

## 5. المسار المهني (Career Timeline)

اعمل Timeline بنفس فكرة "مسارين على الطاولة" (شركات / عمل حر) لو مناسب، أو مسار واحد لو الخبرة كلها فريلانس/مشاريعك الخاصة:

- **الوضع الحالي:** مهندس Backend مستقل — يدير مشاريع متعددة (BoxesVDR, Linkatik, IronLink) بالتوازي مع Binaa Soft.
- **PetroApp:** نظام إدارة مغاسل — كتابة ٦٨٤+ اختبار، CI/CD عبر Jenkins/ArgoCD/Docker، تكامل Datadog APM.
- **مشاريع سابقة:** MatX (منصة تجارة إلكترونية متعددة البائعين)، Ishtiraky، إلخ — اختصر كل واحد بسطرين إنجاز.
- كل بند: (اسم المشروع/الشركة) + (المدة) + (الدور) + (٢-٣ نقاط إنجاز مقاسة برقم).

---

## 6. التعليم

- **بكالوريوس جيوفيزياء** — هذه نقطة قوة سردية مهمة (انتقال ذاتي التوجيه لهندسة البرمجيات)، اعرضها بفخر مش كعيب. ممكن سطر زي: "خلفية علمية في الجيوفيزياء، حوّلتها لمسار Backage هندسي ذاتي التعلّم بالكامل."
- أي شهادات/كورسات تقنية معتمدة (لو موجودة) تتضاف هنا.

---

## 7. ماذا أبني (What I Build) — مكافئ قسم "ماذا أبني" في الموقع المرجعي لكن بمنظور Backend

1. **أنظمة SaaS متعددة المستأجرين** — تصميم قواعد بيانات، عزل بيانات العملاء (tenant isolation)، اشتراكات وفوترة.
2. **بنية تحتية وDevOps** — إدارة سيرفرات AlmaLinux/Nginx، Cloudflare، CI/CD عبر GitHub Actions، مراقبة تكلفة الـ Cloud وتحسينها.
3. **APIs وتكاملات** — REST APIs، تكامل مع خدمات خارجية، تقارير PDF (WeasyPrint، عربي RTL).
4. **الاختبارات والجودة** — كتابة مئات الاختبارات، خطوط CI/CD صارمة (خبرة موثقة من PetroApp).
5. **تكامل الذكاء الاصطناعي في المنتجات** — OCR للفواتير (Koomiz)، ملخصات أخبار بالذكاء الاصطناعي (Ishtiraky)، تكامل LLM (IronLink).

---

## 8. المهارات (Skills) — منظمة زي الموقع المرجعي

- **اللغات:** PHP, JavaScript/TypeScript, SQL
- **الأطر:** Laravel, Filament, Next.js (أساسي)
- **قواعد البيانات:** MySQL, Redis
- **DevOps/Cloud:** Nginx, AlmaLinux, Huawei Cloud, Hetzner, Cloudflare, GitHub Actions, Docker, Jenkins, ArgoCD, Datadog APM
- **المفاهيم:** Multi-tenancy, RBAC, REST APIs, CI/CD, System Design, Testing (TDD/PHPUnit), Cost Optimization
- **أدوات:** Git, GitHub, Postman, WeasyPrint

---

## 9. التوصيات (Testimonials)

- اجمع ٣-٥ توصيات من عملاء/مديرين (Binaa Soft، PetroApp، عملاء BoxesVDR/Linkatik) بنفس أسلوب الكروت (صورة + منصب + اقتباس قصير + رابط "اقرأ كاملة").
- لو مفيش توصيات مكتوبة بعد، دي أول خطوة عملية قبل الإطلاق — اطلبها من ٣-٤ أشخاص اشتغلت معاهم.

---

## 10. صفحة "عني" (About) — نقاط سردية مهمة تميّزك

- بداية غير تقليدية: جيوفيزياء → هندسة برمجيات ذاتية التعلّم.
- خلفية تجارية حقيقية: صاحب محل أدوات وبويات — دا مصدر فهمك العميق لسوق IronLink ودليل الموردين اللي بتبنيهم (رابط قوي بين خبرتك الشخصية والمنتج).
- التعليم المجتمعي: قناة يوتيوب + DevPath.ar — بيثبت إنك مش بس بتبني، كمان بتشرح وتعلّم.
- اهتمامك بالاستثمار الشخصي (اختياري، ممكن يتسيب برا الموقع المهني لو حابب تفصل الشخصي عن المهني).

---

## 11. صفحة تواصل (Contact)

- فورم بسيط (اسم/بريد/رسالة) — Backend بسيط عليه (ده مجالك أصلاً، ممكن تبنيه بـ Laravel API بدل خدمة خارجية).
- روابط مباشرة: LinkedIn, GitHub, بريد إلكتروني, قناة اليوتيوب, DevPath.ar.
- سطر تحفيزي: "أخبرني عن مشروعك أو فرصتك — وسأرد في أقرب وقت."

---

## 12. الجانب التقني للموقع نفسه (Stack اقتراح)

بما إنك Backend Engineer بره التخصص الأمامي، الأفضل تبسيط الواجهة:

- **الخيار الأبسط (موصى به):** Laravel + Blade/Livewire أو Laravel + Inertia + React/Vue خفيف — بيوريك إنك تقدر تبني Full-stack مش بس Backend.
- **الخيار البديل:** Next.js ثابت (Static) لو عايز أداء وتصميم زي الموقع المرجعي بالظبط، مع Laravel API خفيف بس للفورم/الاتصال.
- **الاستضافة:** عندك بالفعل سيرفرات Hetzner/Cloud تحت إدارتك — استضف الموقع عليها بنفسك، ده كمان "مشروع مصغّر" تقدر تذكره كإثبات مهارة DevOps.
- **الدومين:** اقترح: `mahmoudtaha.dev` أو `mahmoudtaha.online` أو ربطه بـ Binaa Soft.

---

## 13. تفاصيل تقنية: ثنائية اللغة + الوضع الليلي/النهاري

### أ) العربي/الإنجليزي (i18n)
- **البنية:** كل المحتوى (Hero، المشاريع، السيرة، المهارات، التوصيات) لازم يتخزن بنسختين (AR/EN) من أول يوم — مش ترجمة لاحقة. لو الـ Stack Laravel + Inertia/Livewire استخدم ملفات Lang أو جدول `translations` لو المحتوى ديناميكي (مشاريع تُدار من لوحة تحكم). لو Next.js استخدم `next-intl` أو `next-i18next`.
- **الاتجاه (RTL/LTR):** تبديل تلقائي لـ `dir="rtl"` مع العربي و`dir="ltr"` مع الإنجليزي على مستوى الـ `<html>`، مع مراعاة أن كل الـ CSS يستخدم خصائص منطقية (`margin-inline-start` بدل `margin-left`) عشان التصميم ميتكسرش بين الاتجاهين.
- **الخطوط:** خط عربي واضح (زي IBM Plex Sans Arabic أو Cairo) + خط إنجليزي متجانس معاه بصريًا (Inter مثلًا)، مع التأكد إن حجم الخط والتباعد متناسقين بين اللغتين.
- **الروابط:** كل صفحة عندها نسخة `/ar/...` و `/en/...`، مع `hreflang` tags في الـ head لتحسين SEO ثنائي اللغة، وزرار "EN" / "عربي" ثابت في الـ Navbar زي الموقع المرجعي بالظبط.
- **السيرة الذاتية (CV):** نسخة PDF بكل لغة، وزرار "تحميل السيرة الذاتية" يغيّر الملف حسب اللغة الحالية.

### ب) الوضع الليلي/النهاري (Dark/Light Mode)
- **الطريقة التقنية:** استخدام CSS Variables لكل الألوان (خلفية، نص، لون أساسي، حدود) بدل قيم ثابتة، مع كلاس `dark` على الـ `<html>` (مثال: Tailwind's `dark:` variant لو Stack يدعمها).
- **منع "الوميض" (Flash of Wrong Theme):** تحميل تفضيل الوضع (من localStorage أو الكوكيز) قبل أول رسم للصفحة (Script صغير Inline في الـ `<head>`) عشان الزائر ميشوفش وميض بين الوضعين لحظة تحميل الصفحة.
- **التصميم لكل وضع:** الوضع الداكن مش بس "قلب الألوان" — لازم مراجعة التباين (Contrast) في الصور، الأيقونات، وألوان الكروت لكل مشروع بشكل مستقل.
- **الافتراضي:** استخدام `prefers-color-scheme` من نظام تشغيل الزائر كقيمة افتراضية أول زيارة، وبعدها يحترم اختيار المستخدم اليدوي دايمًا.
- **الاختبار:** تأكد إن التبديل بين اللغة والوضع (٤ تركيبات: عربي فاتح، عربي داكن، إنجليزي فاتح، إنجليزي داكن) شغالة كلها صح قبل الإطلاق.

---

## 14. خطة التنفيذ (خطوات عملية بالترتيب)

1. جمع المحتوى الخام: أرقام دقيقة لكل مشروع (uptime، تكلفة، عدد مستخدمين/tenants)، صور، السيرة الذاتية المحدثة.
2. طلب ٣-٥ توصيات مكتوبة من عملاء/زملاء.
3. تحديد الـ Stack والدومين والاستضافة.
4. تصميم Wireframe بسيط للصفحة الرئيسية (Hero → مشاريع → مسار مهني → مهارات → توصيات → CTA).
5. إعداد بنية ثنائية اللغة (AR/EN) ونظام الوضع الليلي/النهاري من أول مرحلة بناء (قبل كتابة أي محتوى نهائي) — لأنهم بيأثروا على بنية الـ CSS والـ Routing من البداية، وتأجيلهم بيكلف إعادة بناء لاحقًا.
6. بناء الصفحة الرئيسية أولًا بالنسختين (عربي/إنجليزي) وبالوضعين (فاتح/داكن)، بعدين صفحات المشاريع التفصيلية.
7. صفحة "عني" و"تواصل" بنفس المعايير الثنائية.
8. تحسين SEO الأساسي (meta description، Open Graph، صورة مشاركة) لكل لغة على حدة (hreflang).
9. اختبار الأداء والاستجابة على الموبايل، وكل التركيبات الأربع (لغة × وضع).
10. النشر على سيرفرك الخاص + شهادة SSL (نفس الخبرة اللي عندك من Linkatik).
11. مشاركة الموقع على LinkedIn/GitHub وربطه بقناة اليوتيوب.

---

*ملاحظة: الخطة دي هيكل ومحتوى مقترح مبني على معلوماتك المعروفة عن مشاريعك — راجع كل رقم وإنجاز مذكور وتأكد إنه دقيق قبل النشر.*
