# قائمة ما قبل الإطلاق — الموقع الشخصي

الموقع مبني وشغّال بالكامل (عربي/إنجليزي × فاتح/داكن). الباقي **محتوى**، مش كود.

---

## ١. الأرقام غير المؤكدة (الأهم)

كل رقم لسه محتاج تأكيد مكتوب بين علامتين `«...»` في **`config/portfolio.php`**، وبيظهر على الموقع بخلفية صفراء مُخطّطة عشان يبقى مستحيل ينشر بالغلط.

للعثور عليها كلها:

```bash
grep -n "«" config/portfolio.php
```

مفيش رقم مخترع — القيم اللي في الملف مأخوذة من `personal-website-plan.md` وكلها متعلّمة كـ«غير مؤكدة». **راجع كل واحد قبل النشر.**

لما تخلّص كله، اختفاء الخلفية الصفراء بيحصل تلقائيًا، والاختبار اللي بيراقبها بيتخطّى نفسه لوحده.

### قائمة مختصرة بالمطلوب

| المكان | المطلوب |
|---|---|
| `hero.stats` | عدد منصات SaaS، عدد البيئات، نسبة توفير الـ Cloud، عدد الاختبارات |
| `projects.*.metric` | نتيجة ملموسة برقم لكل مشروع (٦ مشاريع) |
| `timeline.*.period` | تواريخ البداية والنهاية لكل مرحلة |
| `education` | اسم الجامعة وسنوات الدراسة |
| `socials` | روابط LinkedIn / GitHub / YouTube / DevPath الحقيقية |

> الروابط اللي لسه فيها `«...»` **مش بتظهر** في الفوتر ولا صفحة التواصل أصلًا — عشان ما يبقاش فيه لينك مكسور.

---

## ٢. التوصيات

`config/portfolio.php ⇒ 'testimonials'` فاضية عن قصد — مفيش اقتباسات مخترعة.

صفحة `/testimonials` دلوقتي بتعرض رسالة محترمة إنها بيتم جمعها. أول ما تضيف أول توصية، القسم بيظهر تلقائيًا في الرئيسية وفي الصفحة، بالشكل الكامل.

الشكل المطلوب موجود كتعليق جوه الملف نفسه.

---

## ٣. الملفات المطلوبة

| الملف | المسار |
|---|---|
| صورة شخصية (مربّعة، ≥ 480px) | `public/images/portrait.jpg` |
| صورة المشاركة — عربي | `public/images/og-ar.png` |
| صورة المشاركة — إنجليزي | `public/images/og-en.png` |
| السيرة الذاتية — عربي | `public/cv/mahmoud-taha-ar.pdf` |
| السيرة الذاتية — إنجليزي | `public/cv/mahmoud-taha-en.pdf` |
| Favicon | `public/favicon.ico` |

> الصورة الشخصية بتختفي بهدوء لو الملف مش موجود — الصفحة ما بتتكسرش.

---

## ٤. إعدادات الإنتاج

في `.env` على السيرفر:

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://mahmoudtaha.dev    # الدومين النهائي

# الفورم بيبعت إيميل فعلي — لازم SMTP حقيقي
MAIL_MAILER=smtp
MAIL_HOST=...
MAIL_USERNAME=...
MAIL_PASSWORD=...
MAIL_FROM_ADDRESS="noreply@mahmoudtaha.dev"
```

`APP_URL` مهم: منه بتتولّد روابط `hreflang` و `canonical` و `sitemap.xml`.

أوامر النشر:

```bash
composer install --no-dev --optimize-autoloader
npm ci && npm run build
php artisan config:cache && php artisan route:cache && php artisan view:cache
```

---

## ٥. ما تم بناؤه بالفعل

- ✅ ثنائية اللغة كاملة (`/ar` و `/en`) — كل المحتوى مخزّن بالنسختين من أول يوم
- ✅ `dir="rtl"` / `dir="ltr"` تلقائي، وكل الـ CSS بخصائص منطقية (`ms-`, `ps-`, `inset-inline-start`) فمفيش تكسير بين الاتجاهين
- ✅ وضع ليلي/نهاري بـ CSS Variables، بالوتين منفصلتين (مش قلب ألوان)، مع احترام `prefers-color-scheme` وحفظ الاختيار
- ✅ منع وميض الثيم: سكربت inline قبل أي stylesheet (متحقّق منه باختبار)
- ✅ `hreflang` + `canonical` + Open Graph لكل لغة، و `sitemap.xml` تلقائي
- ✅ زرار تبديل اللغة بيفضل في **نفس الصفحة** (حتى في صفحات دراسات الحالة)
- ✅ فورم تواصل شغّال: تحقّق، honeypot ضد البوتات، وحد ٥ رسائل/دقيقة
- ✅ صفحة 404 بتصميم الموقع وبتشتغل حتى من غير locale
- ✅ خط `IBM Plex Sans Arabic` مستضاف ذاتيًا (بيغطي العربي والإنجليزي بنفس المقاييس)
- ✅ احترام `prefers-reduced-motion`
- ✅ **٤٥ اختبار / ١٩٤ تأكيد** — بتغطي التركيبات الأربع (لغة × وضع)، كل المسارات، الفورم، وتماسك المحتوى بين اللغتين
- ✅ PHPStan نضيف، Pint نضيف

### الاختبارات اللي بتحميك

- `ContentTest` — بيفشل لو أي نص اتضاف بلغة واحدة بس
- `ThemeTest` — بيفشل لو أي لون اتعرّف للوضع الفاتح من غير الداكن، أو لو استخدمت خاصية CSS بتكسر الـ RTL
- `LocaleTest` — بيفشل لو زرار اللغة وقّف عن الاحتفاظ بالصفحة الحالية

---

## ٦. الخطوات الجاية (اختيارية)

- `/blog` أو `/writing` (البند الاختياري في الخطة) — البنية جاهزة تستقبله
- لوحة تحكم Filament لو حبيت تدير المشاريع من غير ما تعدّل ملف
- ربط الموقع بقناة اليوتيوب و DevPath.ar

---

## تشغيل محلي

```bash
composer dev     # سيرفر + queue + vite مع بعض
```

الموقع على `http://localhost:8000` → بيحوّل تلقائيًا لـ `/ar`.
